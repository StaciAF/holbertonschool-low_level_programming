#include <stdio.h>
/**
 * main - finds min num of coins as change
 * @argv: array of pointers
 *
 * @argc: argument count
 *
 * Description: returns minimum number of coins
 * Return: number of coins needed
 */



int main(int argc, char *argv[])
{
	int change, i;

	if (argc != 2)
	{
		printf("Error\n");
		return (1);
	}

	if (change < atoi(argv[i]))
	{
		printf("0\n");
	}
}

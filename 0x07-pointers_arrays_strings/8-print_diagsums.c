#include <stdio.h>
#include "holberton.h"
/**
 * print_diagsums - prints the sum of the two diagonals
 * @a: array(matrix) given
 *
 * @size: size of array
 *
 * Description: prints the sum of the two diagonal integers
 * Return: in main
 */

void print_diagsums(int *a, int size)
{
	int i, j, rows, columns;
	sum = 0;
	a = 0;

	for (i = 0; i < size; i++)
		for (j = 0; j < size; j++)
			for (i = 0; i < size; i++)
}
